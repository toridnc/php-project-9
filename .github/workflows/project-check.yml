name: Linter

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # Install project
      - name: Install
        run: make install

      # Run linter
      - name: Linter
        run: make lint